extends ../template/layout

block content

  .row
    .small-12.columns
      #map-canvas

  .row
    .small-12.columns
      p= ps.lat
      p= ps.lng
      script.
        var position = {lat: #{ps.lat}, lng: #{ps.lng}};
