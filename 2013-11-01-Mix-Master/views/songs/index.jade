extends ../template/layout

block content
  .row
    .small-12.columns
  .row
    .small-12.columns
      #songs
        each song in songs
          .song
            a(href='/songs/#{song.id}'): img(src='/images/#{song.art}')
            h4= song.title
